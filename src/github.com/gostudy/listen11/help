1.Go通过package的方式来组织源码


    1 注意：任何一个源码都属于一个包
    2 作用： 代码复用和可读性

2.可执行程序的包名必须为main，并且包含一个main函数

3.除了可执行程序之外，用户可以写自定义包，自定义包编译成静态库
4.导出变量或函数。首字母大写表示可导出，小写表示私有。不能被外部的包访问
5.go install 生成静态库

6.一个包里可以有0个或多个init函数，在程序启动时会被自动调用
    顺序：先执行全局变量--》init函数--》最后执行main
